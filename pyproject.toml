[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "orbit"
version = "0.0.0"
description = "Orbit Django site"
readme = "README.md"
requires-python = ">=3.10, <3.13"
